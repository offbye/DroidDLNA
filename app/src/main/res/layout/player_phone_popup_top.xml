<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_top"
    android:layout_width="fill_parent"
    android:layout_height="60.0dip"
    android:layout_gravity="top"
    android:background="#a52f2f2f"
    android:visibility="visible" 
    android:gravity="center">

    <Button
        android:id="@+id/topBar_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="25.0dip"
        android:background="@drawable/player_bt_phone_back"
        android:paddingLeft="5.0dip"
        android:text="@string/player_bt_return"
        android:textColor="#ffffffff"
        android:textSize="12.0sp" />

    <Button
        android:id="@+id/topBar_list_switch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="25.0dip"
        android:background="@drawable/player_bt_phone_vlist"
        android:textColor="#ffffffff"
        android:textSize="12.0sp"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/video_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:ellipsize="marquee"
        android:gravity="center"
        android:marqueeRepeatLimit="marquee_forever"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:textSize="16sp"
        android:textColor="#ffdadada" />

</RelativeLayout>